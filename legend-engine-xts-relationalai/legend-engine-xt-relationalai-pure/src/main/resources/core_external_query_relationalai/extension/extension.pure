import meta::external::store::rel::contract::*;
import meta::pure::extension::*;

function meta::external::store::rel::extension::defaultRAIExtension(): Extension[1] {
  ^Extension(
    availableStores = [contract()],
    type = 'relationalai'
  );
}