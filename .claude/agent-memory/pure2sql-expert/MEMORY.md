- [Router entry points and dispatch map](router_entrypoints.md) — where `routeFunction`, `StoreContract`, `toSQLQuery`, and `sqlQueryToString` actually live (file paths + symbol names)
- [SQL generator override points per dialect](dialect_generator_overrides.md) — for each of 19 dialect modules under dbExtension/: the extension .pure file, the `<<db.ExtensionLoader>>` registration, and the `createDbExtensionFor<Dialect>` entry
- [Dialect capability matrix from expectedFailures](dialect_capability_matrix.md) — recurring "dialect X can't do Y" shapes observed in PCT expected-failure lists: array_*, date_part, toVariantList, forAll, stdDev/variance, covar, Fold, etc.
- [Extension registration topography (dual SPI)](extension_registration_topography.md) — Pure `<<db.ExtensionLoader>>` + `StoreContract`/`Extension` vs Java `META-INF/services/` (Connection, Compiler, Grammar, Protocol, PCTReportProvider) with concrete file examples
- [PCT categories and ReportScope mapping](pct_categories_reportscope.md) — the 7 PCT categories per dialect, which `ReportScope`/provider feeds each, platform=compiled, H2/Snowflake semistructured sibling suite, modulesToTest.json CI grouping
- [Error-message improvement candidates](error_message_candidates.md) — the `fail(...)`/`assert(...)` call sites behind cryptic PCT-visible messages, with file:line origins, as future targets for actionable wording
- [Build discipline reminders](build_discipline.md) — `mvn clean` mandatory, `-T 4 -DskipTests`, `-am` for scoping, Testcontainers autostart, cloud-only adapters (Databricks/Snowflake) unverifiable locally, IntelliJ gotcha
- [New-feature wiring recipes](feature_wiring_recipes.md) — minimal file sets to touch when adding a scalar vs aggregate vs window function vs new SQL construct vs new dialect, cross-referenced to docs/pct/wiring-howto.md
- [Cursor remap & TDS result-type bugs](cursor_remap_and_tds_result_type.md) — pivot-after-concatenate: isolateTdsSelect findOneNode crash (fixed 2026-04-22), isResultColumnsDynamic false-negative post-isolation (open), SQLExecutionResult null-guard trap
